app_identifier(ENV["IOS_APP_IDENTIFIER"] || "com.example.projectManagementApp")
apple_id(ENV["APPLE_ID"] || "")
team_id(ENV["APPLE_TEAM_ID"] || "")

json_key_file(ENV["SUPPLY_JSON_KEY"] || "")
package_name(ENV["ANDROID_PACKAGE_NAME"] || "com.example.project_management_app")
